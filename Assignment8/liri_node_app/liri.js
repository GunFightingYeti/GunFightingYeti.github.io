// Variables
var songs = require("./spotify.js");
var movie = require("./omdb.js");
var bands = require("./bands.js");
// Input capture
var media = process.argv[2];
var thing = process.argv[3];

// Determine which call is asked for
if (media === "concert-this") {

} else if (media === "spotify-this-song") {

} else if (media === "movie-this") {

}